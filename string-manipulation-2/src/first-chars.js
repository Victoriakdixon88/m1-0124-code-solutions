'use strict';
/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length); //slice  to extract the characters from 0 index to the length,
}
/*length is a vriable or constant representing the desired length of the slice.
//the slice method is used to extract a porition of a sequence, starting from the specified beginning inde up to, but no including, the ending/
//the first parameter, 0, is the starting index of the slice.
//the second parameter, length, represents the ending index of the slice. the slice will include elements up to, but not including this index. */
