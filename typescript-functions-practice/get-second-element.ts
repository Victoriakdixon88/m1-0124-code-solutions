/* exported getSecondElement */
const getSecondElement=(array:unknown[]):unknown => array[1];
//we do not know the name? or is it because we want to pull from multiple arrays that we use unknown? can we use any?
